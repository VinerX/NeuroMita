# Фидбэк Артёма (_atm4x) — прогон 2026-07-03/04

Источник: Discord, ветка начиная с сообщения «ладно, вот это ужас»
(msg `1522730940906868758`). Каждый пункт помечен ID исходного сообщения.

Легенда статусов: `[ ]` — надо сделать, `[~]` — частично/под вопросом, `[x]` — готово.

---

## Установка / AI Hub / бэкенд озвучки

- [x] **1. Плашка «Не забудьте про голоса» почти бессмысленна.**
  (msg `1522731202`, `1522731302`)
  Сделано (вариант A по решению Винера): при включении озвучки (Local) без
  установленного torch-бэкенда — новая плашка `_maybe_warn_backend_missing`
  (`gui/voiceover_controller.py`) с текстом Артёма: «не установлен бэкенд
  озвучки, по видеокарте будет поставлен CUDA/CPU, не согласны — ставьте сами»
  + кнопка «Открыть AI Hub» (категория backend). Показывается раз за сессию.
  Существующие всплывашка про голоса и баннер CUDA не тронуты.

- [x] **2. У установки нет прогресса.** (msg `1522731511`)
  **Найдена настоящая причина:** регулярка `RE_PREP` в `pip_installer.py`,
  которая парсит вывод **uv** `Preparing packages... (X/Y)`, была повреждена —
  `\(` и `\)` превратились в литералы `KATEX_INLINE_OPEN/CLOSE` (артефакт
  markdown/KaTeX-конвейера), поэтому детерминированный прогресс uv никогда не
  ловился → вечное «Ожидание данных о размере пакетов». Исправлено:
  - восстановил `RE_PREP` → `\((\d+)\s*/\s*(\d+)\)`;
  - `overall_percent()` / `has_confident_progress()` теперь берут прогресс из
    счётчика «Preparing (X/Y)», когда uv не отдаёт байтовые размеры (частый
    случай) — бар двигается;
  - в сводке не пишем «Σ 0/0», когда прогресс идёт по счётчику подготовки.
  Тот же артефакт чинил в `voice_action_windows.py` (2 ETA-регулярки).
  **Плюс стадийный прогресс по вехам uv** (`Resolved/Prepared/Installed`,
  regex `RE_RESOLVED/PREPARED/INSTALLED/AUDITED`): стадии resolving→preparing→
  installing→done, полосы 0–10 / 10–90 / 90–100, подписи «Скачивание и
  подготовка пакетов…», «Установка пакетов…», «Установка завершена». Работает
  даже без TTY (uv глушит indicatif-бары при перехвате в пайп, но строки-вехи
  печатает всегда) — `RE_PREP` оставлен как более точный источник, когда он есть.
  Проверено: юнит-тесты (6 passed) + симуляция реальной последовательности uv
  (бар 12→32→90→92→100). Живьём на 166-пакетном прогоне не гонял.
  Ресёрч: машинно-читаемого прогресса у uv нет (astral-sh/uv#3199), парсинг
  вывода — единственный путь; вехи выбраны как TTY-независимый драйвер.

- [x] **3. Ошибка «Не найдена папка models» — хорошо, но сделать понятнее +
  дать действие.** (msg `1522732171`)
  Сделано: в `gui/voiceover_controller.py::_show_loading_dialog` текст теперь
  «Невозможно инициализировать модель — не хватает папки models…» + кнопка
  «Открыть AI Hub» (категория voices) через SHOW_WINDOW.

- [x] **4. Инициализация в фоне не отменяется.** (msg `1522732891`, `1522732905`)
  Сделано в два захода:
  (а) отмена пользователем больше не применяет результат: `_on_finish_loading`
  активирует модель только если ждали именно этот model_id (движок-подпроцесс
  не оборвать, но в приложении отмена реально срабатывает).
  (б) **уточнение Артёма:** плашка «нет папки models» раньше всё равно
  запускала инициализацию — `_show_loading_dialog` теперь возвращает bool, и
  при отмене (нет models) `INIT_VOICE_MODEL` НЕ эмитится (оба места вызова).

- [x] **5. По окончании установки голосов статус не обновляется сразу.**
  (msg `1522734040`, `1522734133`)
  Проверка «долетает» только через ~15 сек.
  Причина ~15с: после установки `refresh(force=True)` реально переопрашивает
  статус КАЖДОГО компонента (файлы/pip/манифест) строго по очереди. Сделано:
  проверки статуса теперь идут в пуле потоков (`installable_controller._on_list`,
  `ThreadPoolExecutor`, порядок сохраняется) — задержка сокращается до времени
  самой долгой одиночной проверки. Плюс задержка стала видимой (см. #6), а не
  «немой». Сэндбокс-статус уже авто-обновляется по Install.TASK_FINISHED (#7).

- [x] **6. Нет блокирующего состояния во время проверки.**
  (msg `1522734205`, `1522734293`)
  Нужно состояние вроде *«проверка файлов»*, чтобы нельзя было лишний раз
  кликнуть «Установить». Проверка должна идти в пуле/параллельно, а не
  «сначала ждём проверку всего».
  Сделано: (а) новое состояние карточки `set_state("checking")` →
  «Проверка файлов…», кнопка выключена; на время `refresh(force=True)` все
  свободные карточки помечаются им (`dialog._set_cards_checking`), клик по
  «Установить» заблокирован; по завершении список пересобирается и состояния
  восстанавливаются. (б) сама проверка распараллелена в пуле (см. #5).

- [x] **Доп. баг (найден при отладке установки Винера): ложный провал
  финальной проверки.** Установка ставит пакеты в `--target Lib` подпроцессом,
  а `check_requirements` в главном процессе проверяет их через `find_spec`.
  Кеш `FileFinder` (`sys.path_importer_cache`) не видел пакет, доставленный в
  `Lib` посреди сессии → «требование не выполнено» → «установка не пройдена»,
  хотя пакет реально стоял (и «повисал установленным» после перезахода).
  **Фикс:** `importlib.invalidate_caches()` в начале `check_requirements`
  ([install_requirements.py](src/core/install_requirements.py:188)). Покрыт
  двумя регресс-тестами (вызов invalidate + поведенческий: пакет, созданный
  после прогрева кеша, теперь находится). Тесты — 8 passed.

- [x] **7. Кнопка «обновить/перечитать с диска» для статусов.** (msg `1522734619`)
  Сделано: кнопка ⟳ в шапке блока «Статус» (`sandbox_page._make_strip` +
  `_refresh_status_panel`), дёргает VOICEOVER_REFRESH / REFRESH_MODEL_PANELS +
  update_status_colors. Авто-обновление по Install.TASK_FINISHED и
  VoiceModel.MODEL_INSTALL_FINISHED (через сигнал в GUI-поток).
  В блоке активной сессии (там где «Модель: Нет настроенных моделей») —
  чтобы после реимпорта не перезапускать программу.
  **Доп. (Винер):** сделать **одну общую кнопку обновления на весь блок
  «Статус»** — она же нужна для ASR/микрофона. И **подцепить авто-обновление
  статусов в конце установки** (по идее возможно).

## Релизы / обновления

- [x] **8. Релизы подгружаются только по клику**, а не в фоне отдельным
  потоком. (msg `1522737132`)
  Сделано: страницы Home/Releases создавались лениво (по клику), поэтому лента
  релизов с GitHub тянулась только при открытии. Добавил прогрев кэша на старте
  отдельным фоновым потоком (`main_window._prefetch_release_feed` →
  `load_news_releases_async`, коалесцирует + кладёт в кэш). `NewsPage` на первом
  показе больше не инвалидирует кэш (`_load_content`), invalidate — только по
  кнопке «Обновить». К моменту открытия страниц данные уже готовы.

- [x] **9. Плашка про обновление занимает лишнее место** и мало полезного.
  (msg `1522737409`)
  Перенести инфо об обновлении прямо в плашки Python/Unity с меткой *NEW*.
  Сделано: убрал отдельный баннер-обнову (`_build_update_banner` удалён). На
  карточках Python/Unity рядом с подписью теперь метка `NEW <версия>`
  (`_set_new_badge`, QSS `LauncherHomeNewBadge`, янтарный под тему обновлений),
  видна только когда по этой части есть обнова. Выбор частей — прежние чекбоксы,
  центральная кнопка так же становится «Обновить», индикатор на меню-кнопке.

## Чат / Песочница

- [x] **10. Долго появляется надпись «думает».** (msg `1522735760`)
  Сделано: индикатор показывался только по `ON_STARTED_RESPONSE_GENERATION`
  (после подготовки контекста). Теперь `send_message` показывает «думает»
  сразу при отправке; повторный ON_STARTED с тем же именем гасится.

- [x] **11. Нет индикатора «озвучивает».** (msg `1522735856`)
  Сделано: новое состояние статус-бара «Озвучивает…» с иконкой динамика
  (`mita_status_widget.show_voicing`), показывается на окно озвучки
  (`audio_controller`, события `SHOW/HIDE_MITA_VOICING`). Отдельное состояние,
  чтобы поздний hide не гасил «думает» следующего сообщения.

- [x] **12. Много «спама» снизу.** (msg `1522736127`)
  Сделано (по идее Винера): числа токенов сокращены до «к» (21.3к), Контекст
  показывается процентом заполнения «~63% (20к/32к)», кеш контекста — процентом
  попадания, стоимости урезаны до 4 знаков, подписи укорочены. `app_window_base
  .update_token_count` + `_fmt_tokens`.

- [x] **13. Чёрный фон у прикреплений.** (msg `1522736597`)
  Сделано: причина — viewport у `QScrollArea` в `ImagePreviewBar` красил свой
  тёмный фон палитры. Явно сделал viewport и контейнер прозрачными
  (`image_preview_widget.py`). Полоса под вводом теперь без чёрного фона.
  (Странность «прикрепления снизу» — оставил как есть, это про расположение.)
  «У прикреплений снова background этот проклятый». У пары элементов (превью
  прикреплений внизу поля ввода) фон **чёрный** — должен быть тёмно-серый,
  а лучше вообще без фона. Плюс странно, что прикрепления показываются снизу.
  Доп. (фидбэк Винера): тот же чёрный фон в онбординг-гайде — строка
  Базовый/Продвинутый/Полный, подсказка уровня и метка «Язык» не имели своей
  поверхности и читались как чёрный поверх тёмного контейнера. Добавил им
  лёгкую подложку `chip_bg` (`guide_widget.py`: `GuideLevelRow`,
  `GuideLevelHint`, `GuideMetaLabel`).

- [x] **Доп. (Винер): «думает» залипает после обрыва соединения с игрой.**
  Нормальный поток гасит статус по событиям успеха/ошибки, но если соединение
  с игрой оборвалось посреди запроса — событий нет и «думает» висит вечно.
  Сделано: страховочный watchdog в `mita_status_widget` (`_arm_watchdog`,
  `_BUSY_WATCHDOG_MS=240с`) на занятых состояниях (думает/сжатие/озвучивает),
  перезапускается на каждую смену состояния, гасит статус на крайний случай.

- [~] **14. Баг: одно изображение превратилось в два.** (msg `1522736693`)
  После обновления истории — норм (msg `1522736911`). F5 при этом работает.
  Симптом «в чате 2, после перезагрузки истории 1» → дублируется на живой
  отправке, в истории копия одна. Вероятная причина: одно изображение попадало
  в `all_image_data` из нескольких источников (ручное прикрепление +
  авто-кадр экрана/камеры). Сделано: дедуп по содержимому перед показом и
  отправкой (`_dedupe_images` в `app_window_base.send_message`).
  Нужен репро для подтверждения: это было прикреплённое пользователем
  изображение или изображение от Миты? (если второе — копнём рендер ответа).

## Статусы / настройки

- [x] **15. Блок «Статус» неинформативен.** (msg `1522738046`)
  Красный/серый/зелёный кружки слева воспринимаются как «пункты», а не как
  индикатор работы. Переделать в сторону предупреждения (жёлтый/красный +
  плашка *«не инициализировано»*). Микрофон — делать красным либо скрывать
  половину его полей за фактом, что он выключен.
  Сделано: в `_SandboxStatusRow` (`sandbox_page.py`) крошечный кружок заменён
  на текстовую плашку-статус: «Активно» (зелёная), «Не инициализировано»
  (жёлтая), «Ошибка» (красная), «Выключено» (серая) — `_apply_chip`,
  `_CHIP_STYLE`. Состояние резолвится как раньше (off/active/init/error).
  Когда подсистема выключена — «что именно» (модель/движок) скрывается, чтобы
  у микрофона/голоса не мозолили глаза лишние поля в выключенном виде.

- [x] **16. Не заполнены description'ы** в других вкладках настроек — половина
  пустует. (msg `1522738274`, `1522738303`, `1522738318`)
  Сделано (саб-агент Sonnet): описания-лидеры добавлены секциям в
  `screen_analysis_settings.py` (6), `game_settings.py` (4),
  `rag_memory_settings.py` (2), `model_interaction_settings.py` (1),
  `language_settings.py` (1). Вкладки без `create_settings_section`
  (API, Персонажи, Озвучка, ASR, Обновления) — другая механика, не трогали.

- [x] **17. Секция настроек персонажей перегружена.** (msg `1522738827`, `1522738858`)
  Переделать stackpanel в полноценную панель: список из 7 Мит как
  раскрывающиеся секции → внутри настройка одной Миты + инфо о создателе
  промпта + сброс истории конкретного персонажа. Красные кнопки сброса всей
  истории — вынести вне секций.
  Сделано (ui.py + logic.py): комбобокс выбора персонажа скрыт (остался
  источником правды для logic.py), а над ним — аккордеон из всех Мит
  (`_build_character_accordion`): одна `InnerCollapsibleSection` на персонажа.
  Раскрытие секции сворачивает соседние, делает персонажа текущим и переносит
  (reparent) в неё общую панель настроек одного персонажа (`_char_config_panel`:
  набор промптов, провайдер, автор/версия/описание набора, история этого
  персонажа + обслуживание). Тумблер «Выбранный/Все» убран — по умолчанию всё
  действует на текущего (`logic._scope()` → "current"). Красные «для всех»
  (сброс истории всех, очистка удалённого) + «all»-обслуживание вынесены в
  отдельную «Опасную зону — все персонажи» вне секций (`_build_danger_zone`).
  Проверено offscreen-смоуком: аккордеон на 3 Митах, sibling-collapse и
  reparent панели между секциями работают.

## Окно / выключение

- [x] **18. Окно слишком большое.** (msg `1522739260`)
  Сделано: `main_window._apply_initial_geometry()` — стартовый размер ужимается
  до ≤92% рабочей области экрана и центрируется (вместо жёсткого 1560×920).

- [~] **19. Выключение через крестик → fatal exception** вместо корректного
  фонового завершения. (msg `1522739518`)
  Точного трейса не было: нативный «fatal exception» валится в stderr, а в
  оконном запуске stderr в никуда → падение не попадало в лог.
  Сделано:
  - `faulthandler` теперь пишет дамп в `NeuroMitaCrash.log` (`__main__.py`),
    плюс `sys.excepthook` / `threading.excepthook` логируют любые
    неперехваченные исключения (в т.ч. потоковые) в основной лог.
  - Конкретное упрочнение вероятной причины: после `shutdown_event_bus()`
    шина больше не пересоздаётся (`get_event_bus` возвращает уже
    остановленную), а `emit`/`emit_and_wait` на остановленной шине — no-op.
    Раньше поздний демон-поток мог поднять новый processor-поток на
    завершающемся интерпретаторе — классический источник fatal-падений на
    закрытии. Тест подтверждает: после shutdown потоки не плодятся (−3),
    диспетч не идёт.
  Осталось: воспроизвести закрытие и, если падение ещё есть, прислать
  `NeuroMitaCrash.log` — тогда добьём корневую причину точечно.

---

## Отмечено как хорошее / рабочее (правок не требует)

- Диалог F5 в целом работает (msg `1522736928`).
- «тето прикольно» — отображение модельки в чате (msg `1522736461`).
- Ошибка про отсутствие папки models по сути правильная (msg `1522732171`).
